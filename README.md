The rar file contains all the files I have written for this project.

You do need to use   isabelle components -u <DIR>   before the Format tool works

use it with isabelle format e.g. isabelle format -D my_test_session
